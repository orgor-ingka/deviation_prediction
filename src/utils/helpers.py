def log_message(message: str):
    """
    Dummy utility function for logging messages.
    """
    print(f"LOG: {message}")

def load_config(config_path: str):
    """
    Dummy utility function to load configuration.
    """
    print(f"Loading configuration from {config_path}")
    return {"key": "value"}